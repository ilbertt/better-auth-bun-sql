{
  "name": "@ilbertt/better-auth-bun-sql",
  "description": "A better-auth database adapter for Bun's built-in SQL module",
  "version": "0.1.1",
  "author": "ilbertt",
  "repository": {
    "type": "git",
    "url": "https://github.com/ilbertt/better-auth-bun-sql"
  },
  "license": "Unlicense",
  "type": "module",
  "engines": {
    "bun": "^1.3.0",
    "node": "please-use-bun"
  },
  "types": "./dist/index.d.ts",
  "files": [
    "src/**/*.ts",
    "dist/*.d.ts",
    "package.json",
    "README.md",
    "LICENSE"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "bun": "./src/index.ts"
    }
  },
  "imports": {
    "#*": "./src/*"
  },
  "scripts": {
    "build": "bun run build.ts",
    "generate:fixtures": "bun run scripts/generate-auth-schema-fixtures.ts",
    "test": "bun --bun vitest run",
    "check:types": "bun run --bun tsc",
    "check:codestyle": "bun run --bun biome check",
    "check:fixtures": "bun run scripts/check-fixtures.ts",
    "check:all": "bun check:types && bun check:codestyle",
    "check:commit-message": "bun run --bun commitlint",
    "fix:codestyle": "bun run --bun biome check --write"
  },
  "peerDependencies": {
    "better-auth": ">=1.6.0"
  },
  "devDependencies": {
    "@better-auth/test-utils": "^1.6.25",
    "@biomejs/biome": "^2.5.6",
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@commitlint/format": "^21.2.0",
    "@commitlint/types": "^21.2.0",
    "@types/bun": "^1.3.14",
    "@types/pg": "^8.20.0",
    "better-auth": "^1.6.25",
    "git-cliff": "^2.13.1",
    "kysely": "^0.29.4",
    "pg": "^8.22.0",
    "typescript": "^7.0.2"
  },
  "packageManager": "bun@1.3.14"
}
