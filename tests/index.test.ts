import { describe, expect, it } from 'bun:test';

describe('@ilbertt/better-auth-bun-sql', () => {
  it('placeholder test until the adapter is implemented', () => {
    expect(true).toBe(true);
  });
});
